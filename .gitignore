# Node / Next
node_modules/
.next/
out/
.static-out/
dist/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
bun-debug.log*

# OS
.DS_Store

# Env
.env
.env.*
!.env.example

# Content (author data + snapshots) — anchored to repo root so it does NOT
# swallow source directories like lib/content/
/content/

# NOTE: public/uploads/ and public/og/ are intentionally tracked — assets
# referenced by content must be in git for the CI deploy build to include them.

# Python
__pycache__/
**/__pycache__/
**/*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/


# Claude session-local files, except committed plans
.claude/*
!.claude/plans/
